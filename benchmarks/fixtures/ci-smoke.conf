pid __PID_FILE__;
listen __LISTEN_PORT__;
metrics_path /status/metrics;

location = /health {
    return 200 ok;
}

location = /proxy/health {
    proxy_pass http://127.0.0.1:__UPSTREAM_PORT__/health;
}
