{
  "environment_name": "release-baseline",
  "canonical_target": "dedicated benchmark target (loopback)",
  "preferred_driver": "wrk on the benchmark target against 127.0.0.1",
  "fallback_driver": "external network path only when explicitly requested",
  "worker_count": 2,
  "config_label": "release-baseline config",
  "policy": "Capture one saved baseline JSON per release tag under benchmarks/baselines/, compare it to the previous release, and generate a markdown report before distribution.",
  "routes": {
    "static": "/health",
    "proxy": "/proxy/health",
    "proxy_payload_64k": "/proxy/payload-64k.bin",
    "proxy_payload_256k": "/proxy/payload-256k.bin"
  }
}
