# Tardigrade starter config for host-native package installs.

listen 8069;
server_name localhost;

root /var/lib/tardigrade/public;
try_files $uri /index.html;

location = /health {
    return 200 ok;
}

location / {
    root /var/lib/tardigrade/public;
    try_files $uri /index.html;
}
